package tree;

public enum CalculatingStrategy {
	/**
	 * Variants of possible operations with numerical data , on the basis of which
	 * certain comparison algorithms are based (given, for example, in the class
	 * {@link Algorithms})
	 */
	AVERAGE, SUM, MAX, MIN
}
